<script src="https://widget.cloudinary.com/v2.0/global/all.js" type="text/javascript"></script>

   {{!------------ moving images here, copy across pages: --}}
    <div>
        <img src="/images/curve1.png" alt="shape1" class="curve1">
        <img src="/images/curve4.png" alt="shape4" class="curve4">
    </div>
    {{!----------- end here--}}

<div class="center padded">
<div class="row center-align">
    <img src="/images/hand.png" alt="website logo" class="signin-logo">
    <h4>{{User.firstname}} {{User.lastname}}</h4>
</div>

        <div class ="bottom" class="col s12 containerRec container createrecbox">
            <h5  class="center">
                Create Recommendation
            </h5 >
            <br>
            <div class="row">
                <div class="col s12 containing">


                    {{!-- DROPDOWN 1 --}}
                    <div class="dropdown input-field recShade">
                        <select id="category-select">
                            <option value="">--------</option>
                            <option value="board&cardgames">Board Games</option>
                            <option value="beauty">Beauty</option>
                            <option value="snacks">Snacks</option>
                            <option value="movies">Movies</option>
                            <option value="videogames">Video Games</option>
                            <option value="restaurants">Restaurants</option>
                            <option value="vacation">Vacation Spots</option>
                            <option value="books">Books</option>
                        </select>
                        <label>Select Category:</label>
                    </div>

                    <br>
                    <div id="createProduct"></div>
                    <br>

                    {{!-- INPUT FIELD 1a --}}
                    <div class="input-field recShade">
                        <select name="" id="product-one">
                            <option value="">--------</option>
                        </select>
                        <label>Select first product: </label>
                    </div>
                    <br>

                    {{!-- INPUT FIELD 1b --}}
                    <div class="input-field recShade">
                        <div id="createProductOne">
                            <input type="text" id="comment-one" placeholder="enter product description">
                        </div>
                    </div>
                    <br>

                    {{!-- UPLOAD BUTTON 1 & SCRIPT--}}
                    <button id="upload_widget_zero" class="colored-button" id="widgetZero">Upload files</button>
                    <script type="text/javascript">
                        var recUrl;
                        var myWidget0 = cloudinary.createUploadWidget({
                            cloudName: 'top3project',
                            uploadPreset: 'yb5bx9uo'
                        }, (error, result) => {
                            if (!error && result && result.event === "success") {
                                console.log('Done! Here is the image info: ', result.info);
                                recUrl = result.info.url
                            }
                        })
                        document.getElementById("upload_widget_zero").addEventListener("click", function () {
                            myWidget0.open();
                        }, false);
                    </script>
                    <br>
                    <hr class="hrclass">
                    <br>

                    {{!-- INPUT FIELD 2a --}}
                    <div class="input-field recShade">
                        <select name="" id="product-two">
                            <option value="">--------</option>
                        </select>
                        <label>Select second product: </label>
                    </div>
                    <br>

                    {{!-- INPUT FIELD 2b --}}
                    <div class="input-field recShade">
                        <div id="createProductTwo">
                            <input type="text" id="comment-two" placeholder="enter product description">
                        </div>
                    </div>

                    {{!-- UPLOAD BUTTON 2 & SCRIPT--}}
                    <button id="upload_widget_one" class="colored-button" id="widgetOne">Upload files</button>
                    <script type="text/javascript">
                        var recUrl1;
                        var myWidget1 = cloudinary.createUploadWidget({
                            cloudName: 'top3project',
                            uploadPreset: 'yb5bx9uo'
                        }, (error, result) => {
                            if (!error && result && result.event === "success") {
                                console.log('Done! Here is the image info: ', result.info);
                                recUrl1 = result.info.url
                            }
                        })
                        document.getElementById("upload_widget_one").addEventListener("click", function () {
                            myWidget1.open();
                        }, false);
                    </script>
                    <br>
                    <hr class="hrclass">
                    <br>

                    {{!-- INPUT FIELD 3a--}}
                    <div class="input-field recShade">
                        <select name="" id="product-three">
                            <option value="">--------</option>
                        </select>
                        <label>Select third product: </label>
                    </div>
                    <br>

                    {{!-- INPUT FIELD --}}
                    <div class="input-field recShade">
                        <div id="createProductThree">
                            <input type="text" id="comment-three" placeholder="enter product description">
                        </div>
                    </div>

                    {{!-- UPLOAD BUTTON 3 & SCRIPT--}}
                    <button id="upload_widget_two" class="colored-button" id="widgetTwo">Upload files</button>
                    <script type="text/javascript">
                        var recUrl2;
                        var myWidget2 = cloudinary.createUploadWidget({
                            cloudName: 'top3project',
                            uploadPreset: 'yb5bx9uo'
                        }, (error, result) => {
                            if (!error && result && result.event === "success") {
                                console.log('Done! Here is the image info: ', result.info);
                                recUrl2 = result.info.url
                            }
                        })
                        document.getElementById("upload_widget_two").addEventListener("click", function () {
                            myWidget2.open();
                        }, false);
                    </script>
                    <br>
                    <hr class="hrclass">
                    {{!-- DONE BUTTON --}}
                    <button id="recDone" class="colored-button" type="submit">Done</button>
                </div>
            </div>
        </div class ="bottom-margin bottom">
</div>