<h1>Create recommendation</h1>
<script src="https://widget.cloudinary.com/v2.0/global/all.js" type="text/javascript"></script>  

<div class="container">

    <div class="input-field col s12">
        <select id="category-select">
            <option value="">--------</option>
            <option value="board&card games">Board Games</option>
            <option value="beauty">Beauty</option>
            <option value="snacks">Snacks</option>
            <option value="movies">Movies</option>
            <option value="video games">Video Games</option>
            <option value="restaurants">Restaurants</option>
            <option value="vacation">Vacation Spots</option>
        </select>
        <label>Select Category: </label>
    </div>

    <br>

    <div id="createProduct">
        
    </div>

    <br>

    <div class="input-field col s12">
        <select name="" id="product-one">
            <option value="">--------</option>
        </select>
        <label>Select first product: </label>
    </div>


    <br>

    <div class="input-field col s12">
        <div id="createProductOne">
            <br>
            <input type="text" id="comment-one" placeholder="Product description">
        </div>
    </div>

    <br>
{{!--CLOUDINARY  WIDGET --}}
<button id="upload_widget_zero" class="cloudinary-button" id="widgetZero">Upload files</button>
<script type="text/javascript">  
var recUrl;
var myWidget0 = cloudinary.createUploadWidget({
  cloudName: 'top3project', 
  uploadPreset: 'yb5bx9uo'}, (error, result) => { 
    if (!error && result && result.event === "success") { 
        console.log('Done! Here is the image info: ', result.info);
        alert("image0 uploaded");
        recUrl = result.info.url 
    }
  }
)
document.getElementById("upload_widget_zero").addEventListener("click", function(){
    myWidget0.open();
  }, false);
</script>
{{!-- CLOUDINARY WIDGET --}}

    <div class="input-field col s12">
        <select name="" id="product-two">
            <option value="">--------</option>
        </select>
        <label>Select second product: </label>
    </div>

    <br>

    <div class="input-field col s12">
        <div id="createProductTwo">
            <br>
            <input type="text" id="comment-two" placeholder="Product description">
        </div>
    </div>

{{!--CLOUDINARY  WIDGET --}}
<button id="upload_widget_one" class="cloudinary-button" id="widgetOne">Upload files</button>
<script type="text/javascript">  
var recUrl1;
var myWidget1 = cloudinary.createUploadWidget({
  cloudName: 'top3project', 
  uploadPreset: 'yb5bx9uo'}, (error, result) => { 
    if (!error && result && result.event === "success") { 
        console.log('Done! Here is the image info: ', result.info);
        alert("image1 uploaded");
        recUrl1 = result.info.url 
    }
  }
)
document.getElementById("upload_widget_one").addEventListener("click", function(){
    myWidget1.open();
  }, false);
</script>
{{!-- CLOUDINARY WIDGET --}}
    <br>

    <div class="input-field col s12">
        <select name="" id="product-three">
            <option value="">--------</option>
        </select>
        <label>Select third product: </label>
    </div>

    <br>

    <div class="input-field col s12">
        <div id="createProductThree">
            <br>
            <input type="text" id="comment-three" placeholder="Product description">
        </div>
    </div>
{{!--CLOUDINARY  WIDGET --}}
<button id="upload_widget_two" class="cloudinary-button" id="widgetTwo">Upload files</button>
<script type="text/javascript">  
var recUrl2;
var myWidget2 = cloudinary.createUploadWidget({
  cloudName: 'top3project', 
  uploadPreset: 'yb5bx9uo'}, (error, result) => { 
    if (!error && result && result.event === "success") { 
        console.log('Done! Here is the image info: ', result.info);
        alert("image2 uploaded");
        recUrl2 = result.info.url 
    }
  }
)
document.getElementById("upload_widget_two").addEventListener("click", function(){
    myWidget2.open();
  }, false);
</script>
{{!-- CLOUDINARY WIDGET --}}
    <br>

    <button id="recDone" type="submit">Done</button>
</div>